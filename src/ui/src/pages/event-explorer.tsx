import { ComingPage } from "@/components/console/coming-page.tsx";

/**
 * Event explorer stub (warren-4ed7 skeleton). The page — every structured
 * event in sequence order with filter / follow / export — lands with
 * warren-24b9, behind the global events query API (warren-5eec).
 */
export function EventExplorerPage() {
	return (
		<ComingPage
			title="Event explorer"
			summary="Every structured event in sequence order: filter, follow, export."
			issueId="warren-24b9"
		/>
	);
}
