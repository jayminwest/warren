{
	"$comment": "Bundle-size ratchet for src/ui/dist/ (warren-5abc, plan pl-7b06 step 13). Frozen ceilings — the ratchet only goes down. Update budgets downward when refactoring; do NOT raise them to land a regression. All sizes in bytes. gzip uses Node zlib defaults (level 6) to match Vite's reporter.",
	"$comment_pl55a3_closeout_2026_05_29": "warren-bfc6 / pl-369d: pl-55a3 (warren-6358, frontend design-system revamp, v0.7.2) is now closed, so the TEMPORARY overhaul ceilings raised across PR #197/#201 (and the stacked per-phase $comment_* bumps) are collapsed back to the real frozen floor. Re-measured from a clean `bun run build:ui` + `bun run check:bundle-size`: raw js 583998 / raw css 40432 / gzip js 172380 / gzip css 7942 (largest chunk == totals since the build emits a single js + single css asset). Budgets set to those measured sizes plus the file's historical ~150-200B headroom. The bulk of the js floor is the Phase 5 motion layer (framer-motion v12 via strict LazyMotion + domAnimation, NOT the full motion proxy). The 'ratchet only goes down' rule is back in force from here — future UI work must hold flat or shrink against THIS floor.",
	"totals": {
		"raw": {
			"js": 584200,
			"css": 40600
		},
		"gzip": {
			"js": 172580,
			"css": 8100
		}
	},
	"largest": {
		"gzip": {
			"js": 172580,
			"css": 8100
		}
	}
}
