import { ComingPage } from "@/components/console/coming-page.tsx";

/**
 * Instance page stub (warren-4ed7 skeleton). The page — boot-resolved
 * configuration, read-only: auth mode, admission policy — lands with
 * warren-e680, behind the instance facts API (warren-2eec).
 */
export function InstancePage() {
	return (
		<ComingPage
			title="Instance"
			summary="Boot-resolved configuration, read-only: auth mode and admission policy."
			issueId="warren-e680"
		/>
	);
}
